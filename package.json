{
  "name": "pax-launchpad",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pax — a pump.fun-style launchpad that turns Twitter/X accounts into coins whose market cap is driven by followers and likes, backed by creator funds from the main PAX coin.",
  "bin": {
    "pax": "src/cli.ts"
  },
  "scripts": {
    "start": "tsx src/cli.ts",
    "cli": "tsx src/cli.ts",
    "demo": "tsx src/demo.ts",
    "test": "node --import tsx --test src/launchpad.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24",
    "tsx": "^4.19.2",
    "typescript": "5.7.3"
  }
}
